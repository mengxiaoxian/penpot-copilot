{
  "name": "json-to-ui-plugin",
  "version": "1.0.0",
  "description": "Penpot 插件：从 JSON 数据自动生成 UI 原型",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "typescript": "^5.3.3",
    "vite": "^5.0.10"
  },
  "dependencies": {
    "@penpot/plugin-styles": "^1.0.0",
    "@penpot/plugin-types": "^1.0.0"
  }
}
